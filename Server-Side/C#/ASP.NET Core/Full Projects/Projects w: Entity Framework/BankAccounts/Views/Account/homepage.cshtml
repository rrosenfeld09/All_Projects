@model UserTransactionViewModel

<h1>Welcome to the homepage, @Model.user.first_name</h1>
<h3><fieldset> Your balance is $@Model.user.balance</fieldset></h3>
<span class = "error">@TempData["Error"]</span>
<form action="/adjustmoney/@Model.user.userid">
    <select name="dollar_amount">
    @{
        for (int i = -500; i <= 500; i+=100)
        {
            <option value="@i">@i</option>
        }
    }
    </select>

    <input type="submit" value = "Withdraw / Deposit">
</form>

<h3>Transactions:</h3>
@foreach(var t in @Model.tranList)
{
    <p>@t.amount</p>
}
<a href="/">Go Home</a>
<a href="/logout">Logout</a>
